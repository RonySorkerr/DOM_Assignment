const seatsLeftDisplay = document.getElementById('seatsLeft').innerText;

const allBtns = document.getElementsByClassName('seat_btn');

for(const eachButton of allBtns){
    const seatNumber = eachButton.innerText;
    
    eachButton.addEventListener('')
}
